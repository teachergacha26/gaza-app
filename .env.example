# Environment Variables for Gaza Relief App
# Copy this file to .env.local for local development
# Add these variables to your Vercel deployment settings

# Security Configuration (Military-grade)
IP_SALT=your_random_salt_string_for_ip_masking_change_this_in_production
SECURITY_MODE=enhanced

# Database Configuration (if using Neon or other database)
# DATABASE_URL=postgresql://username:password@hostname:port/database

# API Keys (if needed for external services)
# Example: STRIPE_SECRET_KEY=sk_test_...
# Example: TWILIO_AUTH_TOKEN=...

# App Configuration
NODE_ENV=production
PORT=5000

# Rate Limiting Configuration
MAX_REQUESTS_PER_IP=50
RATE_LIMIT_WINDOW_MS=900000

# Note: This app currently uses in-memory storage for simplicity
# Military-grade security features include IP masking, rate limiting, and input sanitization